import React from 'react';

export interface SidebarPanelProps {
    children: React.ReactNode;
    title: string;
    index?: number;
}

export default function SidebarPanel({}: SidebarPanelProps) {
    return null;
}
